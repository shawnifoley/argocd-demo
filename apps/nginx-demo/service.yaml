#service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx-demo
  namespace: nginx-demo-ns
spec:
  selector:
    app.kubernetes.io/name: nginx-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
